% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regimes.R
\name{state_occupancy}
\alias{state_occupancy}
\title{Per-observation latent state occupancy from a latent-regime fit}
\usage{
state_occupancy(fit)
}
\arguments{
\item{fit}{A \code{bjlm_regime_fit} (a \code{confusion()} latent-regime fit).}
}
\value{
A data frame with the subject and time columns, one \verb{p_<state>} column
per state (rows sum to 1), and a \code{modal_state} column (the argmax state).
}
\description{
Returns the Rao-Blackwellized posterior probability that each observation was
in each latent state, \eqn{P(s_i = s \mid \text{data})}, obtained by averaging
the forward-backward smoothed marginals over the posterior draws (lower
variance than tallying the sampled paths). Aligned with the original data row
order.
}
\seealso{
\code{\link[=regimes]{regimes()}}, \code{\link[=confusion]{confusion()}}.
}
