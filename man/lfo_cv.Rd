% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfo.R
\name{lfo_cv}
\alias{lfo_cv}
\title{Leave-Future-Out Cross-Validation (LFO-CV) for BJLM Models}
\usage{
lfo_cv(
  object,
  t_var = NULL,
  t_grid = NULL,
  min_tau = NULL,
  k_threshold = 0.7,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{bjlm_fit} object.}

\item{t_var}{Character. Name of the time variable in the outcome dataset. If \code{NULL}
(default), it is parsed from the outcome formula (the second variable on the RHS).}

\item{t_grid}{Numeric vector. The grid of time points at which splits should occur.
Defaults to the sorted unique values of \code{t_var} in the dataset.}

\item{min_tau}{Numeric. The minimum time point to use for the initial model fit.
Observations where time <= \code{min_tau} will be used for training, and prediction starts for
the next time step. If \code{NULL}, defaults to the 2nd unique time point in \code{t_grid}.}

\item{k_threshold}{Numeric. The Pareto k shape diagnostic threshold (default 0.7) to trigger a model refit.}

\item{verbose}{Logical. Whether to print progress messages during evaluation.}

\item{...}{Extra arguments passed to \code{\link[=fit]{fit()}}.}
}
\value{
An object of class \code{bjlm_lfo} containing:
\item{elpd_lfo}{The overall expected log predictive density.}
\item{pointwise}{A data frame with pointwise ELPDs for the evaluated observations.}
\item{diagnostics}{A step-by-step diagnostic logging of Pareto k and refits.}
\item{t_var}{The time variable name.}
\item{t_grid}{The time grid evaluated.}
\item{min_tau}{The baseline evaluation start time.}
\item{k_threshold}{The Pareto k threshold used.}
}
\description{
Performs approximate or exact Leave-Future-Out (LFO) cross-validation for a fitted
longitudinal model (\code{bjlm_fit}) using Pareto Smoothed Importance Sampling (PSIS)
to minimize model refits.
}
