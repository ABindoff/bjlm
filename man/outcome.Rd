% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piped_api.R
\name{outcome}
\alias{outcome}
\title{Add an outcome block to a BJLM model}
\usage{
outcome(
  model,
  formula,
  b0 = NULL,
  b1 = NULL,
  deltas = list(),
  omega = list(),
  rho = list(),
  data = NULL,
  family = gaussian("identity")
)
}
\arguments{
\item{model}{A \code{bjlm_model} object.}

\item{formula}{A two-sided formula. For piecewise models, of the form \code{y ~ tau}
where \code{y} is the outcome and \code{tau} is the time variable. For standard models
(no breakpoints), of the form \code{y ~ covariates}.}

\item{b0}{A one-sided formula for the intercept model (piecewise models only).
Supports random intercepts via \code{(1 | subject)}.}

\item{b1}{A one-sided formula for the initial slope model (piecewise models only).}

\item{deltas}{A list of one-sided formulas for slope-change parameters (piecewise models only).}

\item{omega}{A list of one-sided formulas for breakpoint locations (piecewise models only).}

\item{rho}{A list of one-sided formulas for transition sharpness parameters (piecewise models only).}

\item{data}{A data frame containing all outcome variables.}

\item{family}{Error distribution and link. Currently only \code{gaussian("identity")} is supported.}
}
\value{
The modified \code{bjlm_model} object.
}
\description{
Specifies the outcome model. Can fit either a general piecewise change-point
model or a standard regression (mixed-effects or fixed) using the speed shortcut.
}
