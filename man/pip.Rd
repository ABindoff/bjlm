% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess_bjlm.R
\name{pip}
\alias{pip}
\title{Extract Posterior Inclusion Probabilities from a spike-and-slab bjlm fit}
\usage{
pip(x, ...)
}
\arguments{
\item{x}{A \code{bjlm_fit} object fitted with \code{spike = prior_spike_slab(...)}.}

\item{...}{Unused.}
}
\value{
A data frame of class \code{smoothbp_pip} with columns \code{parameter},
\code{pip}, \code{lower}, and \code{upper} (95\\% credible interval on the inclusion
probability, derived from a Beta posterior).
}
\description{
Computes the posterior inclusion probability (PIP) for each coefficient
that was assigned a spike-and-slab prior. The result carries the
\code{smoothbp_pip} class (from the \pkg{smoothbp} package) so that
\code{\link[=plot.smoothbp_pip]{plot.smoothbp_pip()}} can be used directly.
}
\seealso{
\code{\link[=prior_spike_slab]{prior_spike_slab()}}, \code{\link[=plot.smoothbp_pip]{plot.smoothbp_pip()}}
}
