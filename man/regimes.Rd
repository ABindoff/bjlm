% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regimes.R
\name{regimes}
\alias{regimes}
\title{Add a discrete latent-regime (hidden Markov multistate) component}
\usage{
regimes(
  model,
  name,
  data,
  n_states,
  states = NULL,
  time_var = NULL,
  subject = NULL,
  switch = level ~ 1,
  obs_state,
  obs_model = exact(),
  obs_true = NULL,
  transition = ~1,
  ref_state = 1,
  init = ~1,
  hierarchical = TRUE,
  re = ~(1 | subject),
  priors = regime_priors()
)
}
\arguments{
\item{model}{A \code{bjlm_model} object.}

\item{name}{Character label for this regime block.}

\item{data}{A data frame. In the current phase the observed-state column must
also be present in the outcome data.}

\item{n_states}{Integer number of states K.}

\item{states}{Optional character vector of state names (length \code{n_states}),
used to label the observed indicator and the reported level offsets.}

\item{time_var, subject}{Time and subject-grouping column names (used from the
FFBS phase; recorded now).}

\item{switch}{A formula naming which outcome parameter(s) switch with the
state and their within-state model. Current phase supports \code{level ~ 1}.}

\item{obs_state}{Column name of the observed (in the current phase, exact)
regime indicator.}

\item{obs_model}{Emission model for the indicator: \code{\link[=exact]{exact()}} (current phase)
or \code{\link[=confusion]{confusion()}} (later).}

\item{obs_true}{Optional column marking rows whose state is exactly known
(later phases).}

\item{transition}{One-sided formula for covariate/treatment-dependent
transition intensities (later phases). \code{~ 1} is homogeneous.}

\item{ref_state}{Reference state (name or index) for the level corner
constraint and the transition/initial-state reference.}

\item{init}{Initial-state model (later phases).}

\item{hierarchical, re}{Subject-level random effects on the transition/emission
model (later phases).}

\item{priors}{A \code{\link[=regime_priors]{regime_priors()}} bundle.}
}
\value{
The modified \code{bjlm_model} object.
}
\description{
Adds a discrete latent regime process to the outcome model: the outcome level
switches with a latent state that (in the full model) follows a continuous-time
Markov jump process with covariate/treatment-dependent transition intensities
and is observed through a possibly-misclassified indicator. This generalizes
the smooth change-point (a single monotone transition) to recurring,
stochastic, covariate-driven regime switches — the natural description of
relapsing-remitting disease (e.g. MS phenotype/EDSS states) and staged
progression (e.g. dementia CDR stages).
}
\details{
\strong{Current phase (v0).} Fits a regime-dependent \emph{level} with the
states held fixed at the observed indicator (\code{obs_model = exact()}).
Covariate-dependent transitions (\code{transition}), the misclassification emission
(\code{\link[=confusion]{confusion()}}), and hierarchical subject effects are parsed and validated but
warn that they engage in a later phase. See
\code{data-raw/DESIGN_regimes_hmm.md}.
}
\seealso{
\code{\link[=exact]{exact()}}, \code{\link[=confusion]{confusion()}}, \code{\link[=regime_priors]{regime_priors()}}.
}
