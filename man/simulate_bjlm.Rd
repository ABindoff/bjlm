% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_bjlm}
\alias{simulate_bjlm}
\title{Simulate data from the Bayesian Joint Longitudinal Model (BJLM)}
\usage{
simulate_bjlm(
  n_subj = 50L,
  n_obs = 10L,
  b0 = 50,
  b0_trt = 2,
  b1 = -0.3,
  omegas = c(5),
  rhos = c(5),
  deltas_int = c(-0.2),
  deltas_trt = c(0.4),
  sigma = 1,
  sigma_u = 1.5,
  tau_range = c(0, 10),
  trt_intercept = 0.3,
  trt_x = 0.6,
  gp_confounder = FALSE,
  gp_alpha = 1,
  gp_rho = 3,
  gp_sigma_x = 0.5,
  b0_gp = 2,
  trt_gp = 0.5,
  seed = NULL
)
}
\arguments{
\item{n_subj}{Number of subjects. Default is 50.}

\item{n_obs}{Observations per subject. Default is 10.}

\item{b0}{Baseline overall intercept. Default is 50.0.}

\item{b0_trt}{Causal effect of treatment on the intercept. Default is 2.0.}

\item{b1}{Baseline pre-change-point slope. Default is -0.3.}

\item{omegas}{Vector of change-point locations. Default is \code{c(5.0)}.}

\item{rhos}{Vector of transition sharpness parameters. Default is \code{c(5.0)}.}

\item{deltas_int}{Vector of baseline slope changes at each change-point. Default is \code{c(-0.2)}.}

\item{deltas_trt}{Vector of treatment-specific slope changes at each change-point. Default is \code{c(0.4)}.}

\item{sigma}{Residual standard deviation of the outcome. Default is 1.0.}

\item{sigma_u}{Subject-level random intercept standard deviation. Default is 1.5.}

\item{tau_range}{Range of time variable. Default is \code{c(0, 10)}.}

\item{trt_intercept}{Baseline log-odds of treatment. Default is 0.3.}

\item{trt_x}{Coefficient for baseline confounder in the propensity model. Default is 0.6.}

\item{gp_confounder}{Logical. If \code{TRUE}, simulate a time-varying latent GP confounder. Default is \code{FALSE}.}

\item{gp_alpha}{Marginal standard deviation of the latent GP. Default is 1.0.}

\item{gp_rho}{Lengthscale of the latent GP. Default is 3.0.}

\item{gp_sigma_x}{Observation noise standard deviation for the noisy GP covariate. Default is 0.5.}

\item{b0_gp}{Coefficient for the latent GP on the outcome intercept. Default is 2.0.}

\item{trt_gp}{Coefficient for the baseline latent GP value on treatment assignment. Default is 0.5.}

\item{seed}{Reproducibility seed. Default is \code{NULL}.}
}
\value{
A \code{data.frame} with the generated dataset. True parameters are stored in the
\code{"true_params"} attribute.
}
\description{
A comprehensive data generator supporting multiple change-points, binary treatment assignment,
propensity score confounding, subject-level random intercepts, and continuous latent Gaussian Process
confounders. This function is designed for validation and simulation studies.
}
