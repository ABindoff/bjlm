% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{gp_priors}
\alias{gp_priors}
\title{Priors for the latent Gaussian-process hyperparameters}
\usage{
gp_priors(
  alpha = prior_lognormal(0, 1),
  rho = prior_lengthscale(),
  sigma_x = prior_lognormal(-1, 1)
)
}
\arguments{
\item{alpha}{Prior for the GP marginal SD. A standard-deviation family:
\code{\link[=prior_lognormal]{prior_lognormal()}}, \code{\link[=prior_halfnormal]{prior_halfnormal()}}, \code{\link[=prior_halfcauchy]{prior_halfcauchy()}}, \code{\link[=prior_halft]{prior_halft()}},
or \code{\link[=prior_gamma]{prior_gamma()}}.}

\item{rho}{Prior for the GP lengthscale: \code{\link[=prior_lengthscale]{prior_lengthscale()}} (resolution-aware,
the recommended default), \code{\link[=prior_lognormal]{prior_lognormal()}}, \code{\link[=prior_invgamma]{prior_invgamma()}} (Betancourt's
lengthscale prior), \code{\link[=prior_gamma]{prior_gamma()}}, or \code{\link[=prior_halft]{prior_halft()}}.}

\item{sigma_x}{Prior for the GP observation-noise SD. Same family set as \code{alpha}.}
}
\value{
A \code{gp_priors} object.
}
\description{
Bundle of priors for a latent-GP block (see \code{\link[=latent_gp]{latent_gp()}}). Each prior compiles to
a fast, closed-form log-density on the log scale, so the sampler stays exact and
cannot panic on an arbitrary distribution.
}
\details{
Inverse-gamma is offered only for \code{rho} (directly on the lengthscale); it
is deliberately not offered for the SD slots, where it is conventionally placed on
the variance and would be ambiguous.
}
